<!DOCTYPE html>
<html>
<head>
  <title>Checkawt: The easiest service finder.</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
<%= render 'layouts/header' %>
  <div class="container">
      <% flash.each do |name, msg| %>
      <%= content_tag(:div, msg, class: "alert alert-#{name}") %>
          <% end %>
           <%= yield %>
    </div>

<div class="footer">
	<small>
		Site Design by Magoo
	</small>
</div>

</body>
</html>